# Dependency overrides for APK packages
# Format: package:additional-dependency
# These dependencies are added when repacking from Debian to APK

# ncurses-dev needs ncurses-bin (not in Debian deps but required by APK)
libncurses-dev:ncurses-bin

# Add other missing dependencies here as needed
